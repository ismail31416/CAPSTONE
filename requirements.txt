# --- Core ---
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.27.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0

# --- Perception stack ---
ultralytics>=8.1.0          # YOLOv8 / YOLOv11 detection + pose
easyocr>=1.7.0              # OCR text detection/recognition
opencv-python>=4.8.0
scikit-learn>=1.3.0         # K-means color clustering
Pillow>=10.0.0
matplotlib>=3.7.0           # optional visualization

# --- Data / datasets ---
datasets>=2.18.0            # POPE / VSR loading from the Hub

# --- Optional: faster LLM loading on some GPUs ---
# bitsandbytes>=0.43.0      # for 4-bit / 8-bit quantized backends
# sentencepiece>=0.2.0      # required by some tokenizers (LLaMA, etc.)
